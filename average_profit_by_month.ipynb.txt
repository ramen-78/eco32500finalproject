import pandas as pd
import numpy as np
import matplotlib.pyplot as plt

df = pd.read_csv('profits sales to discount rate.csv')

months = df['Order_Month']
avg_Profit = df['Avg_Profit']

plt.figure(figsize=(10, 6))
plt.plot(months, avg_benefit_per_order, marker='o', linestyle='-', color='orange', label='Avg Benefit per Order')
plt.xlabel('Month')
plt.ylabel('Avg Profit')
plt.title('Average Profit by Month')
plt.xticks(months)
plt.legend()

coefficients = np.polyfit(months, avg_Profit, 1)
trend_line = np.polyval(coefficients, months)

plt.plot(months, trend_line, linestyle='--', color='blue', label='Trend Line')

plt.grid(True)
plt.show()
